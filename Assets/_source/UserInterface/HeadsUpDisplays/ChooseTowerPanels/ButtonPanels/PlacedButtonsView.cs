using UnityEngine;

namespace UserInterface.HeadsUpDisplays.ChooseTowerPanels.ButtonPanels
{
    public class PlacedButtonsView : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
